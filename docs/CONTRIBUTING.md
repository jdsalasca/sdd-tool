# Contributing

## Branching
- Use Gitflow branches: `feature/*`, `fix/*`, `chore/*`.
- Squash-merge into `develop`.

## Commits
- Use Conventional Commits (e.g. `docs: add style guide`).

## Pull requests
- Fill out `.github/PULL_REQUEST_TEMPLATE.md`.
- Include test results or rationale for skipping.

## Docs
- Update `docs/INDEX.md` when adding new docs.
- Place reports under `docs/reports/`.
 - Follow `docs/STYLE.md`.

## Tests
- Run `npm test` before pushing.

## Quickstart
- New contributors should start with `docs/CONTRIBUTOR_QUICKSTART.md`.
- Issue labeling and prioritization rules live in `docs/ISSUE_TRIAGE_PLAYBOOK.md`.
